<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - TrainTix</title>
    <!-- Include Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.5.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body, html {
            /* background-image: url('https://i.pinimg.com/originals/59/92/6d/59926d62445c0dfdfbcd766336cab127.jpg'); */
            height: 100%;
            background-color: #f7f7f7;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
        }
        .full-height {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .card-container {
            width: 100%;
            max-width: 400px;
            padding: 2rem;
            box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
            border-radius: 20px;
            background-color: #fff;
        }
        .card-header {
            font-weight: bold;
            background: #5cb85c;
            color: white;
            border-radius: 20px 20px 0 0;
            padding: 1.5rem 0;
            font-size: 1.5rem;
        }
        .card-body {
            padding: 2rem 0;
        }
        .form-label {
            font-weight: bold;
        }
        .form-control {
            border-radius: 30px; /* Increased border-radius */
            padding: 1rem 1.5rem; /* Increased padding */
            border-color: #ddd; /* Light gray border color */
        }
        .btn-primary {
            background-color: #5cb85c;
            border: none;
            border-radius: 30px; /* Increased border-radius */
            padding: 0.75rem 1.5rem; /* Increased padding */
        }
        .btn-primary:hover {
            background-color: #4cae4c;
        }
        .login-link {
            font-size: 1rem;
            margin-top: 1rem;
        }
        .login-link a {
            color: #5cb85c;
            text-decoration: none;
            font-weight: bold;
        }
        .login-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="full-height">
        <div class="card-container">
            <div class="card">
                <div class="card-header text-center">Login to TrainTix</div>
                <div class="card-body">
                    <% if (error) { %>
                        <div class="alert alert-danger">
                            <%= error %>
                        </div>
                    <% } %>
                    <form method="POST" action="/login">
                        <div class="mb-3">
                            <label for="email" class="form-label">Email</label>
                            <input type="email" class="form-control" id="email" name="email" required>
                        </div>
                        <div class="mb-3">
                            <label for="password" class="form-label">Password</label>
                            <input type="password" class="form-control" id="password" name="password" required>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Login</button>
                    </form>
                </div>
            </div>
            <div class="text-center mt-3 login-link">
                Don't have an account? <a href="/signup">Sign up</a>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.5.0/dist/js/bootstrap.min.js"></script>
</body>
</html>